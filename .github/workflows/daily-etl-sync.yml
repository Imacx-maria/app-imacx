name: Daily ETL Sync (Fast - Last 3 Days)

on:
  schedule:
    # Runs every day at midnight UTC (00:00)
    # Adjust to your timezone if needed:
    # - 0 23 * * * = 11 PM UTC (midnight in Lisbon winter)
    # - 0 1 * * * = 1 AM UTC (2 AM in Lisbon summer)
    - cron: '0 0 * * *'
  
  # Allow manual trigger from Actions tab
  workflow_dispatch:

jobs:
  sync-phc-data:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install ODBC Driver for SQL Server
        run: |
          curl https://packages.microsoft.com/keys/microsoft.asc | sudo apt-key add -
          curl https://packages.microsoft.com/config/ubuntu/22.04/prod.list | sudo tee /etc/apt/sources.list.d/mssql-release.list
          sudo apt-get update
          sudo ACCEPT_EULA=Y apt-get install -y msodbcsql17
          sudo apt-get install -y unixodbc-dev
      
      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install -r scripts/requirements.txt
      
      - name: Run Fast ETL Sync (Last 3 Days)
        env:
          # PHC Database Connection
          MSSQL_DIRECT_CONNECTION: ${{ secrets.MSSQL_DIRECT_CONNECTION }}
          
          # Supabase Connection
          PG_HOST: ${{ secrets.PG_HOST }}
          PG_DB: ${{ secrets.PG_DB }}
          PG_USER: ${{ secrets.PG_USER }}
          PG_PASSWORD: ${{ secrets.PG_PASSWORD }}
          PG_PORT: ${{ secrets.PG_PORT }}
          PG_SSLMODE: ${{ secrets.PG_SSLMODE }}
        run: |
          echo "🚀 Starting Fast ETL Sync (last 3 days with watermarks)..."
          python scripts/etl/run_fast_all_tables_sync.py
          
          if [ $? -eq 0 ]; then
            echo "✅ ETL Sync completed successfully!"
          else
            echo "❌ ETL Sync failed!"
            exit 1
          fi
      
      - name: Notify on failure
        if: failure()
        run: |
          echo "⚠️ ETL sync failed! Check logs for details."
          echo "Failed at: $(date)"
