# =======================
# Supabase Configuration
# =======================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =======================
# PHC Database Connection (SQL Server)
# =======================
MSSQL_DIRECT_CONNECTION=DRIVER={ODBC Driver 17 for SQL Server};SERVER=your-server,1433;DATABASE=your-db;UID=user;PWD=password;TrustServerCertificate=yes;Connection Timeout=10;
MSSQL_DIRECT_USERNAME=your-username
MSSQL_DIRECT_PASSWORD=your-password

# =======================
# Supabase Database Connection (for ETL)
# =======================
PG_HOST=aws-0-eu-west-2.pooler.supabase.com
PG_DB=postgres
PG_USER=postgres.your-project-id
PG_PASSWORD=your-password
PG_PORT=5432
PG_SSLMODE=require

# =======================
# ETL Python Scripts Configuration
# =======================
# Python executable path
PYTHON_PATH=python
PYTHON_ARGS=

# Option 1: Relative path (RECOMMENDED for GitHub/deployment)
ETL_SCRIPTS_PATH=scripts/etl

# Option 2: Absolute path (for local development only)
# ETL_SCRIPTS_PATH=C:\Users\maria\Desktop\Imacx\IMACX_PROD\NOVO\imacx\NEW-APP\imacx-clean\scripts\etl

# Optional: External ETL Service (if using remote ETL instead of local scripts)
# ETL_SYNC_URL=https://your-etl-service.com
# ETL_API_KEY=your-api-key

# =======================
# GitHub Actions Dispatch (fallback when server is not Windows)
# =======================
# Personal access token or repo token with workflow scope
# GH_TOKEN=your-github-token
# Owner and repo to dispatch
# GH_REPO_OWNER=your-org-or-username
# GH_REPO_NAME=your-repo-name
# Workflow file and branch to dispatch
# GH_WORKFLOW_FILE=daily-etl-sync.yml
# GH_REF=main
